# Rails API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://127.0.0.1:3000

# Used in welcome emails for the “Open portal” link (no trailing slash).
# Defaults to http://localhost:3005 in ApplicationMailer if unset.
MARKETER_PORTAL_URL=http://localhost:3005
